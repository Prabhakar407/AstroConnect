# Hosted readiness checkpoint — authentication and routing

## Follow-up: authenticated platform 404 responses

## Resolved database connection; next integration boundary

User supplied the correct redeployment domain astrologer-website-kundan-singh-kvvgyzm2l-neura-flow1.vercel.app and response {"storage_ready":true,"booking_enabled":false}. Record database/schema readiness as observed by the user online, not agent-authenticated proof. Uppercase key and all-environment scope are confirmed by the user; do not repeat those questions.

Continuation check: unauthenticated GET /api/ready on that exact domain returns 302 text/plain; the apex https://astroadvicebykundansingh.com/api/ready returns 404 text/plain. worker.mjs fixes its origin to the apex and rejects redirects. Resend callbacks also need a reachable endpoint. Therefore helper deployment against the apex now would not prove or deliver the intended flow. Source publication alone has not made the new API available at the live domain. Do not deploy a knowingly unreachable helper, introduce bypass tokens in callback URLs, disable protection or merge main without authority. Discuss preview reachability separately from a production release; real mail recipient approval is still needed. No customer form, database write, email or provider mutation was performed in this continuation.

Latest configuration report: user says the database value is saved as Secret for Production, Preview and Development and redeployment completed. Do not keep assuming Preview scope is absent. Exact key casing is not independently confirmed (user writes lowercase in chat; code requires uppercase ASTRO_DATABASE_URL). Current GitHub records still identify 8b4a4d2 / AX3ms4C45KPx1JoXodJY5wZnNFBj and immutable preview astrologer-website-kundan-singh-26r8qiji5-neura-flow1.vercel.app; they do not prove the identity of the user's newer manual redeployment. Fresh branch readiness request still returns authentication redirect to the agent, not an application result. Next obtain the latest deployment's plain generated domain and Source commit from the correct account, not a secret share/bypass link. No new runtime result after the latest save is known; neither ongoing failure nor successful readiness may be claimed. No credential recopy, reset, code workaround or repeated rebuild is justified yet.

User clarification: ASTRO_DATABASE_URL is absent from Vercel altogether, not merely confirmed Production-only. Recovered the existing runtime value through provision.read_config from owner-only operator storage; checked the exact permanent pooled host, neondb, restricted astro_booking_app role and TLS mode without displaying the secret. Copied only that value to Windows clipboard for direct user entry into the website's Vercel settings. No database connection, password reset, account mutation or application change. User must create the exact uppercase key for Preview/design/page-by-page, save, redeploy that preview and report readiness. Production configuration remains a separate pending setting, not assumed present. Clipboard contents are ephemeral and must not be assumed available in later sessions; advise clearing clipboard/history after pasting.

Latest user-reported result after routing correction: /api/health returns {"status":"online","booking_enabled":false}; /api/ready returns {"detail":"Booking storage is not configured.","code":"storage_unavailable"}. Native Python routing now reaches both handlers. storage.py raises this exact message before attempting a connection when its DSN is absent. This is missing runtime configuration, not evidence of failed Neon connectivity. Next user action: make the existing restricted pooled ASTRO_DATABASE_URL available to Preview for design/page-by-page, preserve Production, redeploy that branch preview and recheck /api/ready. Do not expose its value, create another database, change main, or enable checkout. Actual hosted database/schema readiness remains unproved.

The user reports Vercel NOT_FOUND for both /api/health and /api/ready. The branch preview URL was rechecked against the current Vercel GitHub bot link and matches; the deployment is marked Ready. These responses do not establish database failure or successful Python invocation.

Source inspection found cleanUrls enabled alongside explicit /api/index.py and /index.html rewrite destinations. Vercel's current configuration documentation explicitly prohibits extension-bearing rewrite destinations with cleanUrls. Removed the unnecessary cleanUrls option, retaining API-first routing and existing file destinations. Added a hosting regression assertion. No design, credentials, schema, protection, production branch or domain change. This corrects a documented configuration inconsistency; actual hosted routing must still be verified after the replacement preview builds.

Reference: https://vercel.com/docs/project-configuration/vercel-json#rewrites (checked 2026-09-11).

Verification: all four `src.backend.tests.test_hosting` tests pass in the local runtime environment. The restricted runner stalled in its request-thread test and was stopped; the unrestricted rerun completed normally. `git diff --check` passes. This is configuration and application proof, not a simulation of the hosted Vercel router. No UI changed and no new screenshots are claimed. Publish only the existing draft PR branch and observe its replacement preview; do not merge main.

## Earlier checkpoint

Publication follow-up: routing correction 8b4a4d2 pushed only to design/page-by-page. GitHub reports Vercel SUCCESS for deployment AX3ms4C45KPx1JoXodJY5wZnNFBj. Remote main remains 3f09f6b91a272419ba13afba2fa1731c13bc996d. Await authenticated /api/health and /api/ready responses from this replacement preview; build success alone is not runtime proof. This result note remains local for the next work commit, avoiding a documentation-only rebuild.

Rechecked PR #1: OPEN/draft at 90dc96161297cae60e49e5b0d7be4dd823ad668e, Vercel SUCCESS. No merge, source push or deployment in this check.

The browser tool started with a blank session. Navigating directly to the existing branch preview's /api/health redirected to Vercel Login. This establishes that the deployment-protection boundary is reached, not that Python failed. No credential, protection bypass, access request or unrelated Vercel connector was used. No forms, database writes, email, payment or calendar actions were invoked.

Reviewed actual handlers: /api/health returns status online and booking_enabled false without storage; /api/ready checks the packaged migration checksums against the database and returns storage_ready plus booking_enabled false, or a safe storage error. Neither handler writes records or sends notifications. False booking_enabled is expected while checkout remains unimplemented; it is not an instruction to add a switch.

Current user-only check: in the browser signed into the website's Vercel account, open the branch preview URLs ending /api/health and /api/ready and paste their short visible results. No screenshots, passwords, cookies or connection strings are needed. If either renders the website, reports invocation failure or remains at Vercel access denial, report that rather than claiming readiness.

The user has reported saving provider settings in Production. Vercel Preview settings are separate; branch-specific overrides are supported. Their presence/absence has not been independently verified. Do not assume Production values reach this preview, broadly copy all secrets, relax allowed origins, or remove deployment protection. After observed results, configure only the needed existing connection/identity settings for design/page-by-page and the exact preview origin; then redeploy that preview only. Client Google login requires the client's real account and approved Google origins and remains unverified.

References: https://vercel.com/docs/environment-variables and https://vercel.com/docs/deployment-protection/methods-to-protect-deployments/vercel-authentication (checked 2026-09-11).
