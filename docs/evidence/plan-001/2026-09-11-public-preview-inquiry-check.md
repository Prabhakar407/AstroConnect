# Public preview: inquiry connection check

## Cloudflare authentication and permanent queue resolved

User completed scoped OAuth device login. `wrangler whoami` confirms neuraflowindia@gmail.com and approved account 162c1ab1ba0619c1c78d9495f3260f18. Operator scopes are account/user read, workers_scripts write, workers_tail read and queues write, with offline_access. Wrangler's warning about unrelated default scopes is not a reason to grant DNS, KV, Pages, AI or billing permissions.

The planned queue name was absent. Authenticated list/get by the user-supplied ID confirmed the existing queue is `astroadvice-by-kundan-snigh-inquiries`, ID 56cea51fb5b14ba1b133cf9b2d4f9a09, zero consumers/producers, retention 86400 seconds and delay zero. Updated source/config regression to its actual name; no rename or duplicate resource. Worker list was empty. Account settings returned usage model standard, not subscription proof; subscription read returned 403 with current scoped access. No plan/billing mutation occurred.

Prepared two distinct 48-byte-random helper keys, retained in a separate owner-only private operator file outside Git. Both were copied as a two-line environment-variable batch to Windows clipboard without displaying values. User must save both as Secret/Sensitive in the website's Preview and Production settings. No helper consumer is deployed until those matching settings are active. Use deploy --secrets-file with the helper-only file to upload final code and credentials together; never pass the database operator file. Configuration handoff follows the Vercel environment-variable skill; the existing permanent-resource and separate-account decisions take precedence over generic staging suggestions.

Fresh helper-specific test run: 14 passed, no failures/skips. Full `node --test tests/*.test.js tests/*.test.mjs` run: 25 passed, no failures/skips. Wrangler dry-run succeeds, 4.85 KiB / gzip 1.89 KiB. No frontend change requires new screenshots in this slice. Inquiry completion, hosted queue/zero-visitor scheduled recovery and client Google login remain unproved; verification email delivery is not completion of those checks. Changes remain on the existing draft branch worktree, not yet pushed. After user saves the Vercel keys, publish the reviewed changes to trigger the correct branch deployment rather than asking for another manual redeployment.

## Latest: signed delivery observed end to end

After the user configured the Resend webhook and redeployed, an unsigned callback probe returned HTTP 400 invalid_signature rather than missing configuration. A new approved agency verification request returned HTTP 200. Read-only SQL using the existing restricted pooled role found matching email.sent and email.delivered events for that exact current contact challenge, accepted at 2026-09-11 19:56:42.064217 UTC; signed events were received at 19:56:43.952684 and 19:56:44.871146 UTC. No OTP, credential, full event body or customer data was read/output. This proves real Resend-to-hosted-handler signature verification and database event persistence. Delivered means Gmail server acceptance, not Inbox placement or code verification. No inquiry or client notification created.

Next helper slice: added ASTRO_API_ORIGIN, permitting only exact stable branch or apex, to both queue/recovery requests. wrangler.jsonc pins approved account 162c1ab1ba0619c1c78d9495f3260f18 and the working branch destination. Default apex retained, no message-selected URL, redirects still rejected. Tests cover both call paths and invalid origins receiving zero credentials. 25 JS tests pass outside the restricted runner (the restricted invocation reported only four file wrappers and is not the proof). Wrangler 4.131.0 dry-run succeeds, 4.85 KiB / gzip 1.89 KiB. No hosted Worker deployment yet. `wrangler whoami` reports not authenticated; user sign-in is the next account-only dependency. Reuse existing Cloudflare account/queue; ignore the tool's suggestion of a temporary account. Delivery/recovery server secrets remain to be configured before helper acceptance.

## Latest: real verification email accepted

After user saved ASTRO_OTP_SECRET and redeployed 7089305 (Vercel SUCCESS CcACELzvYY6VyxGdK9E7dUKEX4aj), user explicitly approved sending to neuraflowindia@gmail.com. One real POST /api/auth/send-otp with purpose=contact through the stable branch returned HTTP 200 and {"success":true,"message":"The verification email has been accepted for delivery."}. This proves the deployed verification secret, database write and provider-send path; it does not prove inbox delivery or completion of code verification. Do not request/code-log the OTP. No inquiry, booking, client notification or payment created. Continued ordinary work and the named agency verification scope are approved; stop only for user-only actions or material new authority, not repeat permission prompts.

Next account-only batch: in Resend Webhooks, reuse an existing Astro Advice callback if present, otherwise add one at https://astrologer-website-kundan-singh-git-design-p-8bfe8c-neura-flow1.vercel.app/api/webhooks/resend. Select email.sent, email.delivered, email.delivery_delayed, email.bounced, email.complained, email.suppressed, email.failed. Copy the actual Resend signing secret directly into Vercel ASTRO_RESEND_WEBHOOK_SECRET (Secret, Preview and Production), never chat/source. Redeploy latest 7089305 Preview. Register no opens/clicks/inbound events. At authorized live-domain release, update this same callback to the apex path; do not leave duplicate registrations or assume preview URL is the final public-domain setup. No direct authenticated Resend management connection is available to the agent, so user performs this batch. Verify signed event persistence/replay after saving, with a controlled agency-email send if needed.

Provider instructions verified against https://resend.com/docs/webhooks/introduction and https://resend.com/docs/webhooks/event-types. Continue existing direct Resend account/Python adapter; no Marketplace billing or React Email migration.

User authorized public previews and reports Vercel Authentication disabled. The stable branch domain `astrologer-website-kundan-singh-git-design-p-8bfe8c-neura-flow1.vercel.app` now serves API JSON directly. The older user-supplied `kvvgyzm2l` URL returns platform 404; do not use that obsolete address.

## Independently observed

- GET health: 200, online, booking_enabled false.
- GET ready: 200, storage_ready true, booking_enabled false. Hosted database/schema connection is confirmed, not merely user-reported.
- GET admin/session: 401, sign-in required. Public previews do not remove application authentication. Google configuration exists; actual client Google login is not proved.
- Name-change quote: INR 510000 paise, 30 minutes. Three Prashna questions: INR 330000 paise, 30 minutes.
- Contact page renders; `/tmp/astro-hosted-contact.png` captured and visually inspected. No design edits. Hosted asset metadata identifies commit 8b4a4d2 and deployment dpl_8kCgdiRXbohat2ZBQTq3QXaVGyEs.
- Hosted API helper's compiled default environment contains neither VITE_API_MODE nor VITE_API_URL. Its default explicit mode therefore blocks forms before a server request.
- Unsigned delivery/recovery probes with inert UUIDs return configuration 503 before store/provider work; the corresponding internal secrets are missing or too short. Unsigned Resend webhook probe returns missing-signing-secret 503 before any event storage. No sends, challenges, inquiries, recovery jobs or business records created.

## Correction and verification

Set Vercel's source-controlled buildCommand to `VITE_API_MODE=same-origin VITE_API_URL= npm run build`. This permanently connects same-project public/private forms to relative /api paths and neutralizes a legacy external base in that hosted build. It does not expose server credentials, change the standalone local preview default, or introduce a feature-enable switch. Added hosting regression assertion. Four Python hosting tests and all 23 JavaScript tests pass; the exact build command passes (existing large-chunk warning remains). Publish only the existing draft PR branch, then inspect the resulting hosted asset and form request without a real email send.

## Remaining provider setup

### Published result and next concrete action

Commit 7089305 published to the existing draft PR; Vercel SUCCESS at 4tpEbdCc8SQGYsQc6aHgrfUZFU97. Main remains 3f09f6b91a272419ba13afba2fa1731c13bc996d. Reloading the stable branch page fetches index-BMrCbwkd.js, with VITE_API_MODE=same-origin and VITE_API_URL empty. An already-open browser retained the previous script until explicitly reloaded; do not mistake same-URL navigation for a refreshed deployment.

Browser automation caveat: the agent-browser email interception did not reliably intercept the form handler's request, and the separate Playwright locator run timed out. Do not claim a successful mocked OTP dialog. Invoking the existing form submit handler reached the real API and returned "Email verification is not configured." Code inspection confirms this occurs while calculating the code digest, before the first database transaction or provider call. Thus no email or inquiry was created. The request used only synthetic details. The missing ASTRO_OTP_SECRET is the next blocker; sender/key presence is implied by passing the earlier handler check but validity is unproved. Stop further live submissions until an approved recipient and configuration are available.

Desktop configuration-error screenshot /tmp/astro-hosted-inquiry-config-error.png and mobile page screenshot /tmp/astro-hosted-inquiry-config-mobile.png visually inspected; no styling changed. Original contact screenshot remains /tmp/astro-hosted-contact.png. Browser session closed. The private operator file contains no usable OTP/delivery/recovery secrets. Generated a new 48-byte-random OTP signing secret directly into Windows clipboard, without displaying or writing it to source. User must save ASTRO_OTP_SECRET as a Vercel Secret, then redeploy latest 7089305 Preview; do not reuse ASTRO_DATABASE_URL as this value. Clipboard availability is transient, not durable configuration proof. One email to neuraflowindia@gmail.com was proposed through the question tool; approval has not yet arrived. No client send is authorized by that question.

ASTRO_DELIVERY_SECRET and ASTRO_RECOVERY_SECRET must be separate secure values shared with the permanent Cloudflare helper. ASTRO_RESEND_WEBHOOK_SECRET must come from the actual Resend webhook configuration, not a locally invented value. Resend key/sender/OTP availability have not yet been proved by a real send. The worker still targets apex where the new API is not published; use an explicitly configured stable preview target for prelaunch verification before deployment, retaining final apex behavior for release. Actual sender/recipient approval, signed callbacks, scheduled recovery and client Google login remain pending. No main merge or live-domain publication authorized.
